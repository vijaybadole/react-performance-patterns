import LazyRoute from "../lazy-loading/LazyRoute";

export default function LazyLoadingPage() {
    return (
        <div className="page-container">
            <LazyRoute />
        </div>
    );
}
